#file manager by puppet, do not edit
backend default {
<% options.each do |option| %>
  .<%= option %>;
<% end %>
}

include <%= scope.lookupvar('nginx::params::nginx_sites_enabled')%>/<%= name %>.d/vcl_error.vcl
include <%= scope.lookupvar('nginx::params::nginx_sites_enabled')%>/<%= name %>.d/vcl_fetch.vcl
include <%= scope.lookupvar('nginx::params::nginx_sites_enabled')%>/<%= name %>.d/vcl_hash.vcl
include <%= scope.lookupvar('nginx::params::nginx_sites_enabled')%>/<%= name %>.d/vcl_recv.vcl
